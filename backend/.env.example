# --- Supabase (Project Settings > API dans le dashboard) ---
SUPABASE_URL=https://pokjpsmwkkmjhkvfwqpe.supabase.co
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
# Optionnel : le projet signe les tokens en ES256 (JWT Signing Keys) et le backend
# les valide via JWKS. Ce secret HS256 legacy n'est qu'un fallback.
SUPABASE_JWT_SECRET=

# --- Database (Project Settings > Database > Connection string > "Connection pooling", mode Transaction) ---
# Remplacer [YOUR-PASSWORD] par le mot de passe DB défini à la création du projet.
DATABASE_URL=postgresql+asyncpg://postgres.pokjpsmwkkmjhkvfwqpe:@Psaumes91v1--@aws-0-eu-west-2.pooler.supabase.com:6543/postgres

# --- IA ---
MISTRAL_API_KEY=

# --- Divers ---
ENVIRONMENT=development
FRONTEND_URL=http://localhost:5173
